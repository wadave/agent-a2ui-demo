# Bundled Template — Extracted Content

Auto-generated from `template.pptx` (5 slides). The shape ordering here matches what `extract_presentation_inventory` will produce, so you can map directly between this file and the inventory JSON.

## Slide index 0 — layout `CUSTOM_2`

Shape order matches `extract_presentation_inventory` output (sorted top-down, left-right).

- `shape-0` — 'Title /  / subtitle' (7.35" × 1.19")
- `shape-1` — 'Name, date' (3.35" × 0.81")

## Slide index 1 — layout `Blank_12`

Shape order matches `extract_presentation_inventory` output (sorted top-down, left-right).

- `shape-0` — *empty placeholder, fill me* (2.5" × 5.62")
- `shape-1` — 'Table of Contents' (2.14" × 0.3")
- `shape-2` — *empty placeholder, fill me* (4.17" × 0.5")
- `shape-3` — '01' (0.31" × 0.24")
- `shape-4` — *empty placeholder, fill me* (4.17" × 0.5")
- `shape-5` — '02' (0.35" × 0.24")
- `shape-6` — *empty placeholder, fill me* (4.17" × 0.5")
- `shape-7` — '03' (0.37" × 0.24")
- `shape-8` — *empty placeholder, fill me* (4.17" × 0.5")
- `shape-9` — '04' (0.38" × 0.24")
- `shape-10` — *empty placeholder, fill me* (4.17" × 0.5")
- `shape-11` — '05' (0.37" × 0.24")

## Slide index 2 — layout `Blank_7`

Shape order matches `extract_presentation_inventory` output (sorted top-down, left-right).

- `shape-0` — 'Problem' (8.0" × 0.81")

## Slide index 3 — layout `BLANK_1`

Shape order matches `extract_presentation_inventory` output (sorted top-down, left-right).

- `shape-0` — *empty placeholder, fill me* (9.0" × 0.5")
- `shape-1` — *empty placeholder, fill me* (8.31" × 0.56")

## Slide index 4 — layout `CUSTOM_2`

Shape order matches `extract_presentation_inventory` output (sorted top-down, left-right).

- `shape-0` — 'Proprietary & Confidential' (2.3" × 0.34")
- `shape-1` — 'Thank you' (8.02" × 1.52")
- `shape-2` — *empty placeholder, fill me* (2.88" × 0.26")

## Regenerate

Run `python scripts/cli.py analyze resources/template.pptx`, or re-run the snippet at the top of this file's source.
